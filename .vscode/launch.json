{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "lldb",
            "request": "launch",
            "name": "Debug",
            "program": "${workspaceFolder}/target/debug/webview-ipc-app",
            "args": [],
            "cwd": "${workspaceFolder}",
            "preLaunchTask": "xmake: build"
        }
    ]
}